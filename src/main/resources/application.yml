server:
  port: 5000

spring:
  datasource:
    url: jdbc:mysql://workplacersqlserver.mysql.database.azure.com/db
    username: Artemii
    password: asdf6541!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

